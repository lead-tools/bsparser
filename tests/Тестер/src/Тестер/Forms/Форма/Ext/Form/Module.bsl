
&НаСервере
Процедура ПускНаСервере()
	
	ЭтаОбработка = РеквизитФормыВЗначение("Объект");
	ЭтотФайл = Новый Файл(ЭтаОбработка.ИспользуемоеИмяФайла);
	Парсер = ВнешниеОбработки.Создать(ЭтотФайл.Путь + "ПарсерВстроенногоЯзыка.epf", Ложь);
	ЭтаОбработка.Пуск(Парсер);
	
КонецПроцедуры

&НаКлиенте
Процедура Пуск(Команда)
	ПускНаСервере();
КонецПроцедуры
